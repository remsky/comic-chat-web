// Escapes the URL space a year of mistaken `immutable` poisoned; bump to force fonts and the help icon past their daily cache.

const CACHE_BUST = "2";

export function artUrl(path: string): string {
	return `${path}?v=${CACHE_BUST}`;
}
